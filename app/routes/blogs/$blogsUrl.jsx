/**import {useLoaderData} from '@remix-run/react'

export async function loader({params}){
    const {blogsUrl} = params
    

    console.log({blogsUrl})
    }
   
 */

function $blogsUrl() {
  return (
    <div>
      hola
    </div>
  )
}

export default $blogsUrl
